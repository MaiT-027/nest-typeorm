import { Column, PrimaryGeneratedColumn, Entity } from 'typeorm';

@Entity({ name: 'user' })
export class User {
  @PrimaryGeneratedColumn()
  id: number;

  @Column()
  name: string;

  @Column()
  testColumn: string;
}
